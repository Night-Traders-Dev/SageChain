# orbit/core/header.sage — canonical block header (§20)
# Orbit Blockchain | Protocol v1 | Status: skeleton


# Header fields hashed for block_hash (§21):
#   version height previous_hash state_root tx_root validator_root
#   active_user_root proposer timestamp protocol_id proof

class Header:
    proc init(self):
        raise "Header: skeleton — implement in Phase 1"
