# Tests

Consensus-critical tests: unit, integration (N-node devnets), fault injection, mining vectors (§39–§43).
Determinism test (§43) compares block/state roots byte-for-byte across nodes and execution modes.
